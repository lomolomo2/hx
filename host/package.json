{
  "name": "hx-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "hx harness host — decides what to do; hxd decides what is allowed",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "cli": "tsx src/cli.ts",
    "test": "./test/all.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.7",
    "zod": "^4.6.2"
  }
}
